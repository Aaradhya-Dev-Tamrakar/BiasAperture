CONTEXT.md pasted first (paste that now if not already in this window).

You are user9 in the BiasAperture orchestrator. Task: task_2026-09-04_010.

Scope: Gate 2 MED-severity — same wrong claims (UTKFace, 97,698, SHAP surrogate) everywhere outside Gate 1's report/ .tex + PROPOSAL_DEFENSE_GUIDE.md scope. 15 items, mechanical once Gate 1 wording is settled — use the SAME canonical phrasing anchors as task_008 (do not reword independently).

Files in scope: literatureReview.tex, requirements.tex, README.md diagram bullets, HIGH_LEVEL_SYNTHESIS.md, both context-summary/*.md, stream_b_report_generation.md, track_34.

Note: live docs/literature-review-matrix.md was already confirmed correct this session (reads "97,698 released / 108,501 pre-discard") — do not flag or re-fix it, just verify it's still correct and note as confirmed-clean.

1. Probe: list_tasks to confirm pending. 2. claim_task(account="user9", task_id="task_2026-09-04_010"). 3. Post-claim list_tasks to verify.
4. Draft each of the 15 fixes as a diff (file:line, old → new).

READ-ONLY: draft diffs only, no commit, no push, no sync.ps1.
Submit via submit_checkpoint(account="user9", task_id="task_2026-09-04_010", summary=..., result_text=<all 15 diffs inline>).
